{
  "name": "local-cast-authorizer",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --external:@aws-sdk/* --outfile=dist/index.js"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "@aws-sdk/client-secrets-manager": "^3.1048.0",
    "@types/aws-lambda": "^8.10.145",
    "@types/jsonwebtoken": "^9.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
